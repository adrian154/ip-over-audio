// code for creating RRC filter
(data => {
    try {
        module.exports = data;
    } catch(err) {
        window.RRC = data;
    }
})((rolloff, symbols) => {

    const filter = new Array();

});